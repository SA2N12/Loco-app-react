<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Modifier le stock</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <%- include('../partials/nav') %>
        <main class="max-w-md mx-auto p-4 bg-white shadow-md rounded mt-10">
            <h1 class="text-2xl font-bold text-center mb-6">Modifier le stock</h1>
            <form action="/stocks/<%= stock._id %>?_method=PUT" method="post" class="space-y-4">
                <div>
                    <label for="name" class="block mb-2 font-medium text-gray-700">Nom du stock</label>
                    <input type="text" id="name" name="stockName" value="<%= stock.name %>"
                        class="w-full border border-gray-300 rounded p-2" />
                </div>
                <div>
                    <label for="name" class="block mb-2 font-medium text-gray-700">Quantité</label>
                    <input type="number" id="quantity" name="stockQuantity" value="<%= stock.quantity %>"
                        class="w-full border border-gray-300 rounded p-2" />
                </div>
                <div class="flex flex-col items-center space-y-4">
                    <button type="submit" class="bg-orange-500 text-white px-4 py-2 rounded hover:bg-orange-600 w-full">
                        Enregistrer
                    </button>

                    <!-- Lien de suppression discret en dessous -->
                    <a href="#"
                        onclick="if(confirm('Êtes-vous sûr de vouloir supprimer ce stock?')) { document.getElementById('delete-form').submit(); } return false;"
                        class="text-red-500 hover:text-red-700 text-sm underline">
                        Supprimer ce stock
                    </a>
                </div>
            </form>

            <!-- Formulaire caché pour la suppression -->
            <form id="delete-form" action="/stocks/<%= stock._id %>?_method=DELETE" method="post" class="hidden">
            </form>
        </main>
</body>

</html>