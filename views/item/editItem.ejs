<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier l'Item</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <%- include('../partials/nav') %>
        <main class="max-w-md mx-auto p-4 bg-white shadow-md rounded mt-10">
            <h1 class="text-2xl font-bold text-center mb-6">Modifier l'Item</h1>
            <form action="/items/<%= item._id %>?_method=PUT" method="post" class="space-y-4">
                <div>
                    <label for="name" class="block mb-2 font-medium text-gray-700">Nom de l'item</label>
                    <input type="text" id="name" name="name" value="<%= item.name %>"
                        class="w-full border border-gray-300 rounded p-2" required />
                </div>
                <div>
                    <label for="price" class="block mb-2 font-medium text-gray-700">Prix de l'item</label>
                    <input type="number" id="price" name="price" value="<%= item.price %>"
                        class="w-full border border-gray-300 rounded p-2" required />
                </div>
                <div>
                    <label for="category" class="block mb-2 font-medium text-gray-700">Catégorie de l'item</label>
                    <select id="category" name="category" class="w-full border border-gray-300 rounded p-2" required>
                        <% categories.forEach(cat=> { %>
                            <option value="<%= cat %>" <%=item.category===cat ? 'selected' : '' %>>
                                <%= cat %>
                            </option>
                            <% }) %>
                    </select>
                </div>
                <button type="submit"
                    class="w-full bg-orange-600 text-white px-4 py-2 rounded hover:bg-orange-700">Enregistrer</button>
            </form>
        </main>
</body>

</html>